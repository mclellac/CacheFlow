# Dead Code

This document lists unused or dead code identified during the audit.

## Files

- `src/controller.py` (deleted)

## Methods

- `ConfigManager.ensure_default_config` (in `src/config_manager.py`)

## Variables

- `DEFAULT_CONFIG` (in `src/config_manager.py`)
