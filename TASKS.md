# Task List

- [x] Set default colors for all of the layers header bars and body colors.
- [x] Set default text colors: greens for added text, oranges for modified text, and reds for removed text.
- [x] Clean up the preferences UI: use less expander rows on every layer config page (Flattened Default Backend).
- [x] Hide all advanced settings that are not required for CDN (Headers, Overrides, Path Matches).
- [x] Simplify CDN settings to only include Origin Configs (Default + Additional) and Colors.
